import { FormatAccountPipe } from './format-account.pipe';

describe('FormatAccountPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatAccountPipe();
    expect(pipe).toBeTruthy();
  });
});
